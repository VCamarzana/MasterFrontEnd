import { CharCollectionJsonServer } from '@/pods/json-server';
import React from 'react';

export const CharCollectionPageJsonServer: React.FC = () => {
  return <CharCollectionJsonServer></CharCollectionJsonServer>;
};
